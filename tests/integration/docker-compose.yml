version: "3"

services:
  postgres:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: super-secret

  redis:
    image: redis
    ports:
      - "6379:6379"
